/* {{ ansible_managed }} */

object Zone "{{ ic2_zone }}" {
  parent = "master"
  endpoints = [ "{{ ansible_fqdn }}" ]
}

object Endpoint "{{ ansible_fqdn }}" {
  host = "{{ ansible_default_ipv4.address }}"
}
